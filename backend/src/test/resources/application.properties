# Non-datasource test settings. The datasource itself is forced to in-memory H2 via
# @TestPropertySource on the full-context tests, because this machine has ambient
# SPRING_DATASOURCE_* environment variables that outrank file-based properties.
scraper.base-url=http://localhost:4000
scraper.check-sweep-ms=3600000
